<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">

        <!-- Brand: SVG YH logo -->
        <a class="navbar-brand d-flex align-items-center brand-logo" href="/" aria-label="Home">
            <svg viewBox="0 0 48 48" width="48" height="48" aria-hidden="true">
                <rect x="2" y="2" width="44" height="44" rx="10" fill="url(#grad)" />
                <g fill="none" stroke="white" stroke-width="3.6" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M13 12 L19 20 L19 36" />
                    <path d="M25 12 L19 20" />
                    <path d="M29 12 L29 36" />
                    <path d="M29 24 L39 24" />
                    <path d="M39 12 L39 36" />
                </g>
                <rect x="2" y="2" width="44" height="44" rx="10" fill="none" stroke="rgba(255,255,255,.25)"
                    stroke-width="1.2" />
                <defs>
                    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" stop-color="#2f6bff" />
                        <stop offset="100%" stop-color="#7a2dff" />
                    </linearGradient>
                </defs>
            </svg>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="nav" class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link <%= path === '/projects' ? 'active' : '' %>"
                        href="/projects">Projects</a></li>
                <li class="nav-item"><a class="nav-link <%= path === '/about' ? 'active' : '' %>"
                        href="/about">About</a></li>
                <li class="nav-item"><a class="nav-link <%= path === '/contact' ? 'active' : '' %>"
                        href="/contact">Contact</a></li>
                <li class="nav-item"><a class="nav-link <%= path.startsWith('/blog') ? 'active' : '' %>"
                        href="/blog">Blog</a></li>

                <% if (isAdmin) { %>
                    <li class="nav-item"><a class="nav-link <%= path === '/admin/blog/new' ? 'active' : '' %>"
                            href="/admin/blog/new">New Post</a></li>
                    <li class="nav-item"><a class="nav-link <%= path === '/admin/change-password' ? 'active' : '' %>"
                            href="/admin/change-password">Change Password</a></li>
                    <li class="nav-item">
                        <form method="post" action="/logout" class="d-inline">
                            <button class="nav-link btn btn-link p-0" type="submit">Logout</button>
                        </form>
                    </li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link <%= path === '/login' ? 'active' : '' %>"
                                href="/login">Admin</a></li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>

<!-- Flash messages (one-shot) -->
<%- include('alerts') %>